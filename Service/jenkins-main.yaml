apiVersion: v1
kind: Service
metadata:
  name: jenkins-service-main
  namespace: main
spec:
  type: NodePort
  selector:
    app: jenkins
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30411  # Optional: Let Kubernetes assign if not specified
