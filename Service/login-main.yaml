apiVersion: v1
kind: Service
metadata:
  name: login-service-main
  namespace: main
spec:
  type: NodePort
  selector:
    app: login-service
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30410  # Optional: Let Kubernetes assign if not specified
